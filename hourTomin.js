// hour to minutes

function convertHourToMin(hour){
let checkHour = hour*60;

return checkHour;
}

const giveHour= convertHourToMin(4)
console.log(giveHour);

// minutes to hour

function convertMinToSec(hour){
    let checkMin = hour*60;
    
    return checkMin;
    }
    const giveMin= convertMinToSec(5)
    console.log(giveMin);